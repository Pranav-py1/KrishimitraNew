
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Re-applying full public access for hackathon demo with mock authentication.
    // This allows the frontend-only personas to interact with Firestore collections.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
