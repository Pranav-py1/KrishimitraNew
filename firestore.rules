rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal access for authenticated users to support demo personas
    // Deployment Trigger: 2024-05-20T12:00:00Z
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}