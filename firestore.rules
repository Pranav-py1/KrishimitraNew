rules_version = '2';

// Force deployment at: 2025-02-18T17:00:00Z (Hackathon Demo Mode - Public Access)
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}